need to refacture filter values using ai implimentaion then update the database(there is same thign write in diferent forms)
chatbot implimentation:
    identify the core agents(z core,programs,requiremtns) or quary clasification
    clean data usefull manner
    embeding knowladge base meanigful manner
