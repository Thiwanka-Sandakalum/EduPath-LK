# Admin dashboard environment
# Copy to .env.local (recommended) and fill values

# Clerk publishable key (frontend-safe)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here

# Backend API base URL
# Use the port your backend is running on (often 2000 or 5000 depending on backend/.env)
VITE_API_BASE_URL=http://localhost:2000

# Gemini API key for the Analytics AI Report button
# Create your key in Google AI Studio / Google Cloud and put it in .env.local
VITE_GEMINI_API_KEY=
